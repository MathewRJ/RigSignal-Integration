paths:
  - {{spool_dir}}/rigsignal-ebpf_thread-*.ndjson
exclude_files: ['.tmp$', '.gz$']
tags: {{to_json tags}}
parsers:
  - ndjson:
      target: ""
      overwrite_keys: true
{{#if processors}}
processors:
{{processors}}
{{/if}}
