---
description: >
  Ingest pipeline for RigSignal eBPF kernel telemetry metrics.
  Sets @timestamp and validates probe availability metadata.
  eBPF data arrives pre-aggregated at 1-second intervals from the Rust/Aya daemon.
processors:
  - set:
      field: "@timestamp"
      value: "{{{_ingest.timestamp}}}"
      if: "ctx['@timestamp'] == null"

  - trim:
      field: rigsignal.game.name
      ignore_missing: true

  - script:
      description: "Validate session ID is present"
      lang: painless
      source: |
        def sid = ctx.rigsignal?.session?.id;
        if (sid == null || sid.isEmpty()) {
          throw new Exception('rigsignal.session.id is required in eBPF documents');
        }

on_failure:
  - set:
      field: _ingest.pipeline_error
      value: "{{_ingest.on_failure_message}}"
  - set:
      field: _ingest.failed_pipeline
      value: "rigsignal-ebpf"
