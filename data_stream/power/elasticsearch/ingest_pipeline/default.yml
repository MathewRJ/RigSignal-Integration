---
description: >
  Ingest pipeline for RigSignal power metrics.
  Sets @timestamp and validates battery percentage is in valid range.
processors:
  - set:
      field: "@timestamp"
      value: "{{{_ingest.timestamp}}}"
      if: "ctx['@timestamp'] == null"

  - script:
      description: "Validate battery percentage is 0–100"
      lang: painless
      source: |
        def pct = ctx.rigsignal?.power?.battery_pct;
        if (pct != null && (pct < 0 || pct > 100)) {
          throw new Exception('rigsignal.power.battery_pct must be 0-100, got: ' + pct);
        }

on_failure:
  - set:
      field: _ingest.pipeline_error
      value: "{{_ingest.on_failure_message}}"
  - set:
      field: _ingest.failed_pipeline
      value: "rigsignal-power"
