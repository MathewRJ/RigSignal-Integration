paths:
{{#each paths as |path i|}}
  - {{path}}
{{/each}}
exclude_files: ['.tmp$', '.gz$']
tags: {{to_json tags}}
parsers:
  - ndjson:
      target: ""
      overwrite_keys: true
{{#if processors}}
processors:
{{processors}}
{{/if}}
